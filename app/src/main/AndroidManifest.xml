<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wh.customcontrol">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.classroom.Level3MenuActivity"></activity>
        <activity android:name=".ui.activity.herobook.ColorTextActivity"/>
        <activity android:name=".ui.activity.herobook.DragFramLayoutActivity"/>
        <activity android:name=".ui.activity.herobook.DragSquareActivity"/>
        <activity android:name=".ui.activity.herobook.MeasureActivity"/>
        <activity android:name=".ui.activity.herobook.ShineTextViewActivity"/>

    </application>

</manifest>